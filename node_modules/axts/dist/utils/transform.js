function defaults(value) {
    if (this === undefined)
        return value;
    return this;
}
function safes(values) {
    const value = values.find((value) => value === this);
    return {
        defaults: defaults.bind(value)
    };
}
function boolean() {
    if (this === 'null')
        return Boolean(null);
    if (this === 'undefined')
        return Boolean(undefined);
    return Boolean(this);
}
function string() {
    const value = typeof this === 'boolean' || typeof this === 'number' ? Number(this) : this;
    return {
        safes: safes.bind(value)
    };
}
export function transform(value) {
    return {
        safes: safes.bind(value),
        string: string.bind(value),
        boolean: boolean.bind(value),
        defaults: defaults.bind(value)
    };
}
